## C语言学习日志

<!--根据数据结构例题熟悉链表-->

## 一.单链表的应用

### 1.问题描述

​	完成两个多项式的相加操作：已知有两个多项式Pm(x)，Qm(x)，设计算法实现相加的运算，要求用链式结构实现。

### 2.程序实现

```c
#include <stdio.h>
#include <malloc.h>
#include <stdio,h>
typedef struct poly
{
    int exp;
    int coef;
    struct poly *next;
}Pnode,*PLinklist;

int Lint(PLinklist *head)//链表初始化
{
    *head = (PLinklist)malloc(sizeof(Pnode));
    if(*head)
    {
        (*head)->next=NULL;
        return 1;
    }
    else
        return 0;
}

int CreateTail(*PLinklist *head)//尾插法建立链表
{
    Pnode *pTemp,*pHead;
    int c;
    int exp;
    int i =1;
    pHead =*head;
    scanf("%d,%d",&c,&exp);
    while(c!=0)
    {
        pTemp=(PLinklist)malloc(sizeof(Pnode));
        if(pTemp)
        {
            pTemp->exp=exp;
            pTemp->coef=c;
            pTemp->next=NULL;
            pHead->next=pTemp;
            pHead=pTemp;
            scanf("%d,%d",&c,&exp);
        }
        else 
            return 0;
    }
    return 1;
}

void Polyadd(PLinklist LA,PLinklist LB)//两个链表加和
{
    PNode *LA1=LA->next;
    PNode *LB1=LB->next;
    Pnode *temp;
    int sum = 0;
    while(LA1&&LB1)
    {
        if(LA1->exp<LB1->exp)
        {
            LA->next=LA1;
            LA=LA->next;
            LA1=LA1->mext;
        }
        else if(LA1->exp==LB1->exp)
        {
            sum=LA1->coef+LB1->coef;
            if(sum)
            {
                LA1->coef=sum;
                LA->next=LA1;
                LA=LA->next;
                LA1=LA1->next;
                temp=LB1;
                LB1=LB1->next;
                free(temp);
            }
            else
            {
                temp=LA1;
                LA1=LA1->next;
                free(temp);
                temp=LB1;
                LB1=LB1->next;
                free(temp);
            }
        }
        else
        {
            LA->next=LN1;
            LA=LA->next;
            LB1=LB1->next;
        }
    }
    if(LA1)
        LA->next=LA1;
    else
		LA->next=LB1;
}

void Print(PLinklist head)//打印内容
{
    head=head->next;
    while(head)
    {
        if(head->exp)
            printf("%d^%d",head->coef,head->exp);
        else
            printf("%d",head->coef);
        if(head->next)
            printf("+")
            else
                break;
        head=head->next;
    }
}

void mian()
{
    PLinklist LA;
    PLinklist LB;
    Lint(&LA);
    Lint(&LB);
    CreateTail(&LA);
    CreateTail(&LB);
    Print(LA);
    Print(LB);
    Polyadd(LA,LB);
    printf("\n");
    Print(LA);
}
```

