```#pragma once


typedef struct zhan {
	int val;
	int min;
	struct zhan *next;
} MinStack;

/** initialize your data structure here. */

MinStack* minStackCreate() {
	MinStack *q;
	q = (MinStack*)malloc(sizeof(MinStack));
	q->next = NULL;
	q->val = 0;
	q->min = 0;
	return q;
}

void minStackPush(MinStack* obj, int x) {
	MinStack *q;
	q = (MinStack*)malloc(sizeof(MinStack));
	q->next = obj->next;
	q->val = x;
	if (obj->next == NULL) obj->min = x;
	else {
		if (obj->min > x)
			obj->min = x;
	}
	q->min = obj->min;
	obj->next = q;
}

void minStackPop(MinStack* obj) {
	if (obj->next == NULL) return;
	MinStack *q;
	q = obj->next;
	obj->next = q->next;
    if(obj->next!=NULL)
	obj->min = obj->next->min;
	q->next = NULL;
	free(q);
}

int minStackTop(MinStack* obj) {
	return obj->next->val;
}

int minStackGetMin(MinStack* obj) {
	return obj->min;
}

void minStackFree(MinStack* obj) {
	free(obj);
}

/**
 * Your MinStack struct will be instantiated and called as such:
 * MinStack* obj = minStackCreate();
 * minStackPush(obj, x);

 * minStackPop(obj);

 * int param_3 = minStackTop(obj);

 * int param_4 = minStackGetMin(obj);

 * minStackFree(obj);
*/
```